# 墨衍 · 会话交接文档 v4（2026-08-29 深夜）

> **开新对话：把本文件 + `墨衍-交接文档.md`（主文档，已同步 v4 状态）发给 AI 即可无缝继续。**
> 本文档 = 2026-08-29 全天会话的完整导出（承接 v3 模型迁移版）。当天从「PP-Structure 精修中断点」
> 一路推进到「uni-app 双前端骨架 + 微信开发者工具导入运行」，全部真机验证，无未验证的半成品。

---

## 一、当日成果总览（按序，全部有真机证据）

| # | 板块 | 交付物 | 状态 |
|---|---|---|---|
| 1 | 解析 | 6 份自考真题 PP-Structure 精修（28/28 页，0 失败）；工具 v2（200dpi+段崩隔离+断点续跑）与 v2.1（标题重拼） | ✅ 已验收留用 |
| 2 | 教学引擎质量 | P1 整改：判定注入选项/评分对齐/weak_points 兜底/计划 schema 修复/审计列（修 5 真因，含 2 次误判自我纠正） | ✅ 真机前后对照 |
| 3 | 教学引擎功能 | 脚手架阶梯 hint_level(0-3)+fading、裁判四维评分、socrates-skill 三点并入、施压检测五类 | ✅ 真机验证 |
| 4 | 性能 | 出题∥讲解流、巩固题∥裁判、裁判 cheap+note 限字+采样改（158s→5.6s，-96%）、SQLite WAL、常驻 perf 观测 | ✅ 实测 |
| 5 | 人物系统 | 「同桌」角色卡定案（V2 五件套+世界书 5 条滴灌）、开场白三态带真实档案、试聊 7 轮 | ✅ 架构师亲测 |
| 6 | 双前端地基 | CORS 白名单（默认关）+ `docs/API契约-双前端-2026-08-29.md`（含小程序 enableChunked 样例） | ✅ 46/46 |
| 7 | 前端 v0 | `backend/static/tutor.html` 教学控制台（SSE 流式/判定徽章/选项点选/路线图/档案/续学） | ✅ 浏览器即用 |
| 8 | uni-app 骨架 | `frontend/`（Vue3+Vite，@dcloudio alpha 对齐版），选课页+同桌聊天页，双传输条件编译 | ✅ H5+mp-weixin 构建全过 |
| 9 | 小程序验收 | mp-weixin 包导入开发者工具成功，项目「墨衍」运行，模拟器渲染选课页 | ⏳ 交互验收剩人工 2 分钟（见四） |

**文档产出（docs/，均 2026-08-29）**：教学实录×2（修复前/修复后）、引擎审计、GitHub 调研（脚手架/苏格拉底）、
教学引擎流程图.html、人物设计 D11+候选库+v2+v3+v4、API 契约-双前端。共 11 份。

**工具产出（tools/）**：exam_ppstructure_md（v2 主控）、exam_pp_worker（逐页 worker）、exam_pp_rehead（v2.1 重拼）。

---

## 二、当前系统状态（交接瞬间）

- **后端**：5001 运行中（本次会话全部代码生效；`MOYAN_AI_MOCK` 未开=真引擎；SQLite+WAL）。
- **前端**：①浏览器控制台 `http://127.0.0.1:5001/tutor.html` 即用；
  ②uni-app H5：`cd frontend && npm run dev:h5`（5173，/api 已代理）；
  ③小程序包：`frontend/dist/build/mp-weixin`（urlCheck=false 已生效）。
- **微信开发者工具**：2026-08-29 深夜验收时运行中、已导入项目「墨衍」（模拟器渲染选课页）；
  **接盘时进程未开**，做四-1 交互验收前需重开工具（服务端口仍关闭，CLI 调用需先在 设置→安全设置 开启）。
- **数据**：真题 MD=PP-Structure 版（Docling 备份在 `…_docling备份\`）；
  DB 会话：`s_4c96ef0d2b`（修复前留档）、`s_0b5b073e32`（修复后全流程）、`s_1132c4be46` 等。
- **测试**：46/46（含 CORS 2 例、人物卡 6 例）。

---

## 三、关键定案与根因存档（防重蹈）

1. **PP-Structure 300dpi 必段崩**（server_det 原生 access violation）→ 用 200dpi；faulthandler 抓现场。
2. **判定质量根因**：提示词不传选项文本 + 无 weak_points 要求 + score 无对齐——修在
   `prompts.py`/`judge.py::_harmonize`，并保留程序级兜底（模型偷懒时自动补）。
3. **「全文」计划退化根因**：KnowledgePlan.chapter_id/chapter_title 必填致 LLM 调用失败**静默落 mock**
   → schema 改默认空 + 落 mock 打 warning。静默落 mock 是本项目最危险坑（三处已加日志）。
4. **人物定案「同桌」**：陪伴者不是服务者——TA 有自己的错题本/模考分/错题要问（关系双向）。
   抽象概念贴标签（水灵/墨影/灯灵）和纯形容词人设都会人机；活感=身份来历+台词样本+缺点。
5. **性能真相**：judge 本体 7~32s 波动（网关），优化收益在结构（并行化/采样/裁剪输出），
   已有常驻 perf meta（judge_ms/review_ms/quiz_ms）持续采数据。

## 四、挂账（按优先级）

1. **微信开发者工具交互验收**（剩人工 2 分钟）：**先重开工具并打开项目「墨衍」**，模拟器里 点教材→选「26.4 数据库原理与技术(1).md」
   →点章节→选「数据库原理与技术」→开始学习→聊天页发一句话，验证同桌开场白与教学流。
   （自动化受阻记录：CUA 对该 Electron 窗口坐标派发 identity mismatch；元素通道正常但模拟器
   区域无 a11y 元素；人机共用鼠标互相干扰——动态窗口相对点击法已验证可发点，需窗口静置。）
2. **试聊第二轮回归**：4 缺陷修复后的体验验收（重点：讲解不再尾随追问、提示不再 □、换站有报站）。
3. **第三步部署件**：鉴权（wx.login code2session→users 表）+ 每用户限流（免费限量）+ HTTPS/备案域名
   + CORS 收紧正式域名。当前 API 无鉴权，只可在本机/可信环境使用。
4. 复习"复习即考"（D4 缺口）；判定/出题 usage 偶发缺失；切章器把 `<!-- 页注释 -->` 当标题（ch0 碎章）；
   gpt-5.6-luna 网关稳定后复测主引擎备选。
5. uni-app 细节：微信项目 AppID（现游客模式，云开发等不可用但本地编译/预览可用）；真机预览。

## 五、本次新增坑（后端/工具链，必读）

- **pydantic 禁 setattr 未定义字段（重犯）**：挂运行时值必须先在 schema 定义（exclude=True）。
- **写 PowerShell .ps1 含中文必须带 BOM**（或全 ASCII），否则 PS5 按 ANSI 读→语法炸。
- **bash heredoc 内嵌 Python 转义**：`\\n` 到目标文件变真换行——生成代码用脚本文件，勿内嵌大段。
- **curl 发中文 JSON**：`-d` 会被本地码页坑，用 `--data-binary @file`（文件以 UTF-8 写）。
- **Git Bash 的 /tmp 与 Windows Python 路径不通**：临时文件放项目内。
- **日志排障用全量**：`tail -c` 截断会误判崩溃点（本次误判 FormulaNet 一整轮）。
- **乱码显示别当真**：Git Bash ls/管道显示乱码≠数据坏，用 Python `os.listdir`/ascii() 权威确认。
- **CUA 对 Electron 窗口**：全屏帧易 stale（页面动画重绘）、坐标派发报 identity mismatch；
  可用通道=a11y 元素 AXPress + PowerShell OS 级点击（SetCursorPos/mouse_event）+ 动态窗口矩形
  相对定位（GetWindowRect 实时+比例落点）。人机共用鼠标时勿自动化。
- **WeChat DevTools CLI**：服务端口关闭时 CLI 不可用；`echo y |` 管道喂不进确认（非 TTY）；
  需 GUI 设置→安全设置开启（localstorage `security.enableServicePort` 可直接改，改后重launch）。
- **tasklist 过滤中文进程名**：`//FI "IMAGENAME eq 微信开发者工具.exe"` 可用；杀进程前先
  `netstat -ano` 确认端口归属，勿盲杀。

## 六、下一步候选（新对话从这里接）

1. 完成微信开发者工具交互验收（四-1，2 分钟人工）→ 同桌试聊第二轮回归（四-2）；
2. 第三步部署件（鉴权/限流/HTTPS，部署阶段）；
3. 8c 试聊遗留的第二轮打磨（按架构师试聊体感迭代同桌卡——Monika 17 版模式）；
4. 复习即考 / usage 补齐 / luna 复测（4.2 原列）。

---

## 附：当日时间线（关键节点）

- 上午：接手 v4 交接 → 4.1 诊断（E1/E2 实验+faulthandler）→ v2 工具重写 → 28/28 页全量 → 交接文档更新
- 午前：教学 API 演示 7 轮（抓出判定误判+记忆链饿死）→ 查库坐实真因 → 修 5 处 → 修复后对照实录
- 午后：架构审计（引用/效益/冗余）→ GitHub 调研（脚手架/苏格拉底）→ 三流程图
- 傍晚：性能整改（基线实测→并行化→裁判提速→WAL→perf 观测）
- 深夜：人物设计四轮迭代 → 同桌定案+实装+试聊 7 轮 → 4 缺陷修复 → uni-app 骨架+双目标构建
  → 微信开发者工具导入运行（本文档收尾）
