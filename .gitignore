# 运行时数据（上传/生成的中间产物不入库）
data/
# 本地密钥
.env
# 虚拟环境（1G+，可随时重建）
.docling-venv/
.ocr-venv/
# 前端依赖与构建产物（npm install / npm run build:mp-weixin 可重建）
frontend/node_modules/
frontend/dist/
# 参考资料库（外部参考，不入版本库）
参考项目/
# 杂项
out/
.video_agent/
.zcode/
.pytest_cache/
pytest-cache-files-*/
.workbuddy/
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
# 部署件本地状态
deploy/caddy/
# 测试 SQLite（conftest 动态建，schema 与开发库可能不同）
backend/tests/test_dev.db
