# 教学实录 · 《数据库原理与技术》真题卷（真机 API）

- 时间：2026-08-29　|　服务：http://127.0.0.1:5001　|　引擎：main = deepseek-v4-flash（OpenGo 网关，真机非 mock）
- 会话：`s_4c96ef0d2b`　|　文档：`20260829-131456-abab19`（PP-Structure 精修版 MD，2834 字，单章「全文」）
- 知识点计划：仅 1 个（`ch0/kp-01 全文`）——无标题 MD 导致规划器退化的实况
- 学生由 AI 扮演；`判定原始` 为结构化解析结果（correctness_level=None / score=0.5 即 4.2-P1 bug 的证据）
- 流式输出的 reasoning-delta（模型思考链）不在本实录，原始流在 `data/work/exam_pp_diag/turn1-4.sse`

---
## 第 1 轮 · 空输入触发开讲

**墨衍老师：**

【讲解】  
教材里没有“全文”这个知识点，我不编造。你上传的《教材》目前显示的是试卷开头部分，包括注意事项和两道选择题。  
如果你是想让我讲解试卷中的某个具体概念，比如“数据的物理独立性”或“删除异常”，我可以一次讲一个。你想先看哪一个？

**题目**（ch0/kp-01/q1，easy）：数据的物理独立性是指

- A. 应用程序与数据库中数据的逻辑结构是相互独立的
- B. 用户的应用程序与存储在磁盘上数据库中的数据是相互独立的
- C. 数据库与数据库管理系统相互独立
- D. 用户程序与数据库管理系统相互独立

（讲解流延迟 5400ms）

（本轮结束：`finish_reason=question`）

---
## 第 2 轮 · 答第 1 题（答对）

**学生：** B。物理独立性是说用户的应用程序和存储在磁盘上数据库中的数据相互独立，物理存储方式变了，应用程序也不用改。

**墨衍老师：**

你对物理独立性的理解很到位，能说出“物理存储方式变了，应用程序也不用改”这个关键点，说明你掌握了核心思想。
不过，你选择的选项和你的解释并不一致。请再仔细看看每个选项的表述，哪个选项真正对应你刚才说的意思？
物理独立性强调的是应用程序与哪一层之间的独立？是数据库管理系统，还是磁盘上的数据存储方式，还是数据的逻辑结构？

判定原始：`correctness_level=None`，`score=0.5`，`decision=practice_question`，`confidence=0.8`，`weak_points=[]`，`misconceptions=null`

**题目**（kp-01/q1，medium）：下列哪种现象属于关系规范化中的删除异常？

- A. 数据重复存储，浪费存储空间
- B. 插入一个元组时，必须同时插入其他无关信息
- C. 更新一个元组时，需要修改多处
- D. 删除一个元组时，丢失了其他不该删除的信息

（本轮结束：`finish_reason=question`）

---
## 第 3 轮 · 答删除异常题（故意答错）

**学生：** A，数据重复存储浪费空间就是删除异常。

**墨衍老师：**

你提到了数据重复存储，这确实是规范化要解决的问题之一，说明你关注到了数据冗余。
但删除异常并不是指重复存储浪费空间，而是指在删除某些数据时，意外地丢失了其他本应保留的信息。
请思考：如果在一个表中同时存储了学生信息和选课信息，当删除某门课程时，会不会把学生信息也一起删掉？这种现象属于什么异常？教材里没有“全文”这个知识点，我不编造。你是指试卷中的某个具体概念或题目吗？可以告诉我你想弄明白哪一处，我再按教材帮你梳理。

判定原始：`correctness_level=None`，`score=0.5`，`decision=reteach`，`confidence=0.8`，`weak_points=[]`，`misconceptions=null`

**题目**（ch0/kp-01/q1，easy）：数据的物理独立性是指

- A. 用户程序与数据库管理系统相互独立
- B. 用户的应用程序与存储在磁盘上数据库中的数据是相互独立的
- C. 应用程序与数据库中数据的逻辑结构是相互独立的
- D. 数据库与数据库管理系统相互独立

（讲解流延迟 3776ms）

（本轮结束：`finish_reason=question`）

---
## 第 4 轮 · 再答物理独立性（选项全文作答）

**学生：** 我的答案是：用户的应用程序与存储在磁盘上数据库中的数据是相互独立的。也就是数据的物理存储改变了，应用程序不需要变。

**墨衍老师：**

你的回答完全正确，而且你补充的解释很到位：物理独立性正是当数据的物理存储结构改变时，应用程序无需随之修改。
你能进一步说说，物理独立性与逻辑独立性（即应用程序与数据逻辑结构相互独立）之间的区别吗？

判定原始：`correctness_level=None`，`score=0.5`，`decision=skip`，`confidence=0.8`，`weak_points=[]`，`misconceptions=null`

（本轮结束：`finish_reason=exam`）

---
## 会话落库汇总（/api/study/{doc}/stats）

- 判定 3 次、turns 8、tokens：prompt 2780 + completion 1151 = **3931**（真机成本）
- weak_points low/mid/high 全 0、skills 0 —— 记忆链因 4.2-P1 归一化 bug 未入账
- 会话已持久化：`POST /api/tutor/turn {"session_id":"s_4c96ef0d2b","user_text":"..."}` 可直接续学（下一轮为章末考）