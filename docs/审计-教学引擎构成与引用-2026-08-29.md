# 墨衍 · 教学引擎构成与引用审计（2026-08-29）

> 范围：backend 全部 + tools + 参考项目 + requirements；方法：全量 import 普查 + 调用链追踪 + 死代码验证。

## 一、构成（~5,008 行后端 / 38 单测）

```
routers(5 路由, 539行) → engine(~1,700行) → models(repo 438行) → storage/config
                          ├─ providers(170) OpenAI 兼容抽象 + 流式
                          ├─ router(156) 主备 failover + 熔断
                          ├─ structured(145) 自研结构化输出（json_object+围栏剥离+别名+usage）
                          ├─ judge(141) / quiz(147) / proofread(123) / reviewer(76)
                          ├─ tutor/(service 183 + actions 321 + session 46) FSM
                          └─ review/service(169) 复习会话（自评+失败回收）
services: docling_adapter / pdf_parser / ocr_engine / lines_pipeline(430) / chapter_splitter
数据：SQLite(开发)/PG(生产) 7 张表 + FSRS 卡片字段
```

## 二、引用清单（三类）

### A. 代码级依赖（requirements.txt 14 项）

| 依赖 | 引用方式 | 效益 | 判定 |
|---|---|---|---|
| **fsrs>=6.3**（官方 py-fsrs） | `repo.py:7` 直接 import Card/Rating/Scheduler/State | 记忆调度核心：免自研 21 参数模型，review_due 实测工作 | ✅ 最高性价比 |
| **openai SDK** | providers.py（OpenAI 兼容协议，网关实测可用） | 全部 AI 调用的底座 | ✅ |
| **docling**（.docling-venv，subprocess） | `tools/docling_worker.py`，config.DOCLING_WORKER | 解析主引擎，12 格式 | ✅ 刻意环境隔离 |
| **rapidocr_onnxruntime + onnxruntime + pillow** | ocr_engine.py | 扫描件兜底 OCR（legacy 路径） | ✅ 决策保留 |
| **pymupdf** | pdf_parser / lines_pipeline / 渲染 | 文本层+页渲染 | ✅ |
| fastapi/uvicorn/sqlalchemy/pydantic/pydantic-settings/multipart/psycopg2 | 常规 | — | ✅ |
| **instructor>=1.15** | **全库零 import（仅 4 处陈旧注释提及）** | 曾用，D1 修订被自研 structured.py 替代（reask 65s 延迟+usage 不可审计） | ❌ **死依赖，删** |

（PaddleOCR/paddle 不进 requirements：.ocr-venv 仅工具用，定向精修决策。）

### B. 方法论借鉴（引用思想，代码自研，参考仓库零 import）

| 来源 | 落地物 | 效益 | 判定 |
|---|---|---|---|
| synapse-skills（参考项目 447KB，仅读） | strategy_logs 表 + `best_strategy` 数据驱动换讲法（actions.py:112,207）+ /strategy-stats + /traces | 机制在线且有单测（test_reviewer 8 例）；收益待数据积累（min_samples=3） | ✅ 已实装 |
| engram 思想 | 复习会话自评制 + 失败回收（review/service.py：again→片段重讲→留队） | 已实装，接 3 个 API | ✅ |
| Mr.-Ranedeer + 教育调研（PNAS/MathTutorBench） | 提示词体系：硬/软规则分离、8 硬规则、宽松苏格拉底（D3） | 今日实测护栏有效（不编造/不泄答案） | ✅ |
| Vercel AI SDK Stream Protocol | SSE 类型化事件协议（providers EV_*） | 已实装；微信双传输留口（前端未做） | ✅ |
| instructor | 被替代（见上） | **净负效益**，留下死依赖+文档债 | ❌ 教训已记 |
| LangChain/LangGraph | 规划文档提及，代码零引用 | 决策：手写轻量 FSM | ✅ 正确决策，无残留 |

### C. 归档参考项目（参考项目/，152MB，零运行时引用）

| 项目 | 大小 | 状态 | 判定 |
|---|---|---|---|
| PaddleOCR-PDF | 147MB | 评估期源码归档；评估已出结论、工具已跑通 | 归档/可删（架构师拍板） |
| cangjie-skill蒸馏 | 4.9MB | "教学法技能包方法论备选"——未见实质吸收痕迹 | 备选未启用，冗余候选 |
| synapse-skills | 447KB | 思想已吸收（见 B） | 留（小） |

## 三、冗余与债清单

1. **instructor 死依赖**（requirements.txt）——删（唯一仍安装却零引用的包）。
2. **backend/db.py（88 行）**：第一代 DB 层（Document 模型旧版），全仓零引用（models/db.py 现役）——删。
3. **陈旧文档 5 处**：engine/__init__.py、container.py:29、prompts.py:5、proofread.py:5、requirements.txt 注释仍写"instructor 结构化输出"——改 structured.py 口径。
4. **tools/ OCR 时代一次性脚本**：winocr.ps1（Windows OCR 已弃用）、bench_ocr、run_full_ocr、resume_ocr、render_pdf_pages、merge_chunks、rapid_ocr_worker——归档候选（现役：docling_worker/ocr_convert/import_legacy_docs/model_bench/guardrail_run/exam_*）。
5. **不算冗余（有意保留）**：text-layer/RapidOCR/Docling 三级解析兜底链（决策在案）；config.py+settings.py 两层（路径常量 vs 唯一事实源，后者 20 处引用、前者 12 处）；judge/quiz/plan 三处 mock（无 key 演示+故障兜底——plan mock 曾静默顶班，今已加 warning）。

## 四、体检附带发现（功能缺陷，非冗余）

- 【P2】**章末考出题传空上下文**：actions.py:273 → quiz.py:137 `make_question(context="")` → 违反"只依据教材"红线（模型凭空编题）。
- 【P2】讲解文本可泄漏下一题选项字母（见交接文档 4.2-2）。
- 【P3】Router 主备当前同网关同模型（cheap=main=deepseek-v4-flash），failover 实际是同模型重试——备引擎差异化待 gpt-5.6-luna 复测。

## 五、总评

分层清晰（routers→engine→models→storage 单向依赖）、DI 显式装配、护栏有单测。冗余面小：
**1 个死依赖 + 88 行死模块 + 5 处陈旧注释 + ~152MB 归档**，无重复造轮子。主要风险不在冗余，
而在"静默落 mock"类可观测性（本次已补 plan/judge/quiz 三处 warning）与章末考空上下文。
