# 教学实录（修复后）· 《数据库原理与技术》真题卷

- 时间：2026-08-29　|　会话：`s_0b5b073e32`　|　文档：`20260829-140119-cba7e4`（重拼后带标题结构 MD，ch1 2826 字）
- 知识点计划（真 LLM 生成，16.8s）：数据库系统的基本概念 / 数据的物理独立性 / 自然连接 / 完全函数依赖 / 删除异常
- 本轮验证点：①字母答对判 correct ②score 与分档对齐 ③skip 反馈不悬空 ④答错入账薄弱点（FSRS 队列活了）

---
## 第 1 轮 · 空输入开讲 kp1「数据库系统的基本概念」

**墨衍老师：**

【讲解】
你上传的教材片段里没有直接给出“数据库系统的基本概念”的定义，我不能凭记忆编造。不过片段中有相关题目，比如第17题提到数据管理技术经历了“人工管理、文件系统、____”三个阶段。我们可以从这一个点开始。你记得第三个阶段是什么吗？

**题目**：数据库系统中最重要的用户是（ ）。
- A. 终端用户
- B. 数据库管理员(DBA)
- C. 应用程序员
- D. 系统分析员

（`finish_reason=question`）

---
## 第 2 轮 · 答对（字母 B）→ skip 推进 kp2

**学生：** B，数据库管理员 DBA。DBA 负责数据库的建立、维护和权限管理，是最重要的用户。

**墨衍老师：**

回答正确！你准确指出了数据库管理员在数据库系统中的核心地位。
接下来我们继续学习数据库系统的其他概念。【讲解】数据的物理独立性，简单说就是：**用户的应用程序，和数据库在磁盘上怎么存数据，互不干扰**。  
教材里对应的是选项C——应用程序与存储在磁盘上的数据库中的数据相互独立。  
比如：数据库文件从一块硬盘挪到另一块，或者换了存储格式，只要逻辑结构不变，你的应用程序代码就不用改。  

【提问】那如果数据库的存储设备升级了，但应用程序没做任何修改，这能说明物理独立性存在吗？为什么？

判定归一化结果：`{"correctness": "correct", "score": 1.0, "decision": "skip", "confidence": 0.8, "weak_points": []}`

**题目**：数据的物理独立性是指
- A. 用户程序与数据库管理系统相互独立
- B. 用户的应用程序与数据库中数据的逻辑结构相互独立
- C. 用户的应用程序与存储在磁盘上的数据库数据相互独立
- D. 数据库与数据库管理系统相互独立

（`finish_reason=question`）

---
## 第 3 轮 · 故意答错（A）→ 换讲法 + 薄弱点入账

**学生：** A。物理独立性就是用户程序和数据库管理系统相互独立，程序不依赖 DBMS 运行。

**墨衍老师：**

你注意到程序与数据库系统之间存在独立性，这个方向是值得肯定的。
但物理独立性强调的是应用程序与存储层数据之间的隔离，而不是程序与DBMS之间的独立性。
想一想：当磁盘上的数据存储方式改变时，用户的应用程序是否需要跟着修改？这种‘不受影响’指的是哪两层之间的关系？【讲解】
数据的物理独立性，简单说就是：**用户的应用程序，和数据库在磁盘上怎么存数据，是互不影响的**。  
也就是说，哪怕你把数据文件挪了位置、换了存储结构，只要逻辑结构不变，应用程序都不用改。  
教材里这道题问的就是这个概念，四个选项里只有一项符合这个描述。  

我先不说是哪一项，你来判断：  
“应用程序与数据库中数据的逻辑结构相互独立”算不算物理独立性？为什么？

判定归一化结果：`{"correctness": "incorrect", "score": 0.1, "decision": "alternative_explanation", "confidence": 0.95, "weak_points": [{"skill_id": "db/architecture/physical-independence", "mastery": "low"}]}`

**题目**：数据的物理独立性是指
- A. 用户的应用程序与存储在磁盘上数据库中的数据是相互独立的
- B. 数据库与数据库管理系统相互独立
- C. 用户程序与数据库管理系统相互独立
- D. 应用程序与数据库中数据的逻辑结构是相互独立的

（`finish_reason=question`）

---
## 修复效果对照

| 验证点 | 修复前（s_4c96ef0d2b） | 修复后（s_0b5b073e32） |
|---|---|---|
| 知识点计划 | 1 个「全文」（静默落 mock） | 5 个真知识点（真 LLM，16.8s） |
| 字母答对 | 被判 partial_correct + 幻觉「不一致」 | correct / 1.0 / skip |
| score | 恒 0.5 | 与分档对齐（1.0 / 0.1） |
| 答错入账 | weak_points 空、档案 0 条 | db/architecture/physical-independence: low 落档 |
| 复习队列 | review_due=0 | review_due=1 |
| skip 反馈 | 以提问结尾且被章末考悬空 | 一句话肯定并自然过渡 |

## 遗留（P2，未修）

- 讲解文本可能泄漏下一题选项字母（教材上下文=真题卷自带选项；本轮 kp2 讲解曾提「选项C」，kp3 重讲已自行规避。修法：讲解提示词加「不得引用选项字母」或讲解与出题的上下文隔离）
- 切章器把 `<!-- 第 N 页 -->` 页注释当标题（ch0 变 10 字碎章）；出题/判定其余链路正常
- 本轮 tokens：8,286（3 轮，含讲解流式）