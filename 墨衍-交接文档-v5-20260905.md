# 墨衍 · 交接文档 v5.1（2026-09-05）

> **v5.1 变更（M4 里程碑交付后）**：git 基线已推送对齐；教学端点新增归属校验（404）与并发锁（409）；限流 key 匿名回落 IP；书库审核 fail-closed + shared 下架机制；新增 /api/privacy、/api/admin/smoke；生产实况修正（Python 3.12 / nginx 反代 / Caddy 已弃用）。新坑见第 7 节 13-16 条。

> **用途**：本仓库将由新 AI 团队接手继续开发。**微信小程序端与网页端将分两条线独立开发**，
> 每条线的会话只需阅读：第 1 节（总览）+ 第 5 节（对应端现状）+ 第 6 节（API 契约）+ 第 7 节（关键坑，必读）。
> 本文档取代 v4（2026-08-29）；历史细节见 `墨衍-交接文档-v4-20260829.md` 与 `docs/`。

---

## 1. 项目总览

**墨衍** = AI 同桌式自考辅导平台。用户上传教材（PDF/MD），AI 解析出章节，然后扮演"同桌"
陪学：讲解→提问→判定→施压/支架→错题本，全程 SSE 流式。

- **技术栈**：后端 FastAPI(Python 3.13) @5001；微信端 uni-app(Vue3) 单工程；网页端独立 Vue3+Vite 工程 `frontend-web/`
- **生产**：https://moyan.baimo7715.top （阿里云 ECS 8.136.152.54，nginx 443→uvicorn:5001，PG16）
- **本地 dev**：`run.py` 起 5001，SQLite `data/moyan_dev.db`；H5 dev 在 5173（vite 代理 /api→5001）
- **质量基线**：pytest 161/161 全绿；生产台账 ai_usage/page_views 全链记账
- **业务状态**：个人主体小程序（无支付/类目受限），C 端收费被卡死；网页端免登录共享书库已上线并获真实用户
- **成本纪律**：AI 调用走三档引擎（main/fallback/cheap），所有调用经 ai_usage 台账记账；**新增 AI 功能必须挂限流**（改端点曾因无限流被刷 AI 账单）

---

## 2. 系统架构（必须先懂的五层）

```
SSE 入口(routers/tutor.py /start /turn)
  → 状态机 TutorState(engine/tutor/state.py)：explain→quiz→judge→…
  → 动作层 TutorActions(engine/tutor/actions.py)：每种动作产 SSE 事件流
  → 规则层(engine/prompts.py + persona 常量)：提示词与人设，刚完成"放松"改造
  → 模型层(engine/providers.py)：Router 主备 failover；chat/chat_stream/cheap 三档
  + 安全网 D11：选择题打码(_sanitize_feedback)、confidence<0.5 复核、Reviewer 5 轮抽样
```

**SSE 教学协议**（双端共用，事件 type）：
`start / reasoning-delta / text-delta / finish / meta / judge / question / question-batch / report / error`
- `EV_TEXT = "text-delta"`（providers.py:20），前端只消费 text-delta 展示正文；reasoning-delta 仅维持"思考中"态
- H5：fetch + ReadableStream；小程序：wx.request + enableChunked；api.js 同一份 onEvent 回调

**引擎参数**（backend/settings.py，2026-09-05 刚参数化）：
`scaffold_max_level=3 / solicit_loose_threshold=3 / reteach_switch_threshold=2 / reviewer_sample_every=5`
人设规则已放松：插话先接住再回主线、短讲不设硬上限、explain 注入插话 user_note。

---

## 3. 已完成功能清单（截至 commit 4f72eea）

| 板块 | 内容 | 状态 |
|---|---|---|
| 文档解析 | docling PDF→MD 章节切分（离线运行，339s/104页） | ✅ 生产 |
| 教学引擎 | 五层全链 + 判定四维评分 + 脚手架阶梯 + 施压检测 + 规则放松 | ✅ 生产 |
| 鉴权 | 微信 jscode2session + JWT(HS256,7d) + 网页免登录(Bearer>X-Device-Id>web_anon) | ✅ 生产 |
| 权限 | ADMIN-01/02/03 分层（MOYAN_ADMIN_OPENIDS）；AUTH_DISABLED=0 硬校验 | ✅ 生产 |
| 网页 v2 | 免登录共享书库 + 上传 + 搜索 + content_hash 去重 | ✅ 生产 |
| 管理台 | /admin 口令登录 + 看板/台账/文档管理；删除已收敛至此（用户层无删除） | ✅ 生产 |
| 改名 | REN-01：用户改名过 AI 相符审核(422 拒绝, fail-open)，同名短路，L_HEAVY 10/min | ✅ 生产 |
| 向量库 | Phase5 MVP：切片+JSON 向量+余弦检索+管理员建索引，教学注入默认关(VEC-04) | ✅ 生产(默认关) |
| 用量观测 | ai_usage 台账(自动记账) + page_views 双端埋点(小程序埋点在 0.2.2 待提审) | ✅ |
| 内容安全 | 上传 AI 审核(黄赌毒拒收 fail-open) | ✅ |

---

## 4. 仓库地图

```
backend/
  routers/        documents.py(上传/改名/删除) tutor.py(/start /turn SSE) auth.py admin.py
  engine/         providers.py(Router+Provider) tutor/{state,actions}.py prompts.py judge.py
                  title_check.py(改名AI审核) settings.py(全局参数,env_prefix MOYAN_)
  models/         SQLAlchemy 模型；documents/teaching_sessions/turns/judgements/weaknesses
                  ai_usage/page_views/document_chunks …
  tests/          161 例；test_engine_softening.py(放松+P0回归锁) test_d11_fixes.py(安全网锁) test_boundaries.py(26行边界)
frontend/         uni-app 单工程（微信小程序 + H5 双编译）
frontend-web/     网页端独立工程（Vue3+Vite，/dist 生产已部署 /var/www/moyan-web）
docs/             API契约-双前端-2026-08-29.md（46 例）、REQUIREMENTS（Phase1-6 全 done）、引擎审计等
out/              工作脚本（SFTP 部署/SSH 执行器/压测），gitignore
```

---

## 5A. 微信小程序端（独立开发线一）

**代码**：`frontend/`（uni-app 单工程，条件编译 `<!-- #ifdef MP-WEIXIN -->` 等）
**编译**：`npm run dev:mp-weixin` → 产物 `frontend/dist/build/mp-weixin` 导入微信开发者工具
**工具**：微信开发者工具装在 `D:\微信web开发者工具`，CLI=wechatide.cmd（勿重复下载）
**AppID**：`wx9ca2b10b07573b3d`（个人主体；Secret 在 .env 不入库）
**域名**：request/uploadFile 均已配 `https://moyan.baimo7715.top`

**现状**：
- 书架(共享书库/搜索/上传去重)/教学聊天页(SSE enableChunked)/错题本 已可用
- 版本 **0.2.1 正在微信审核中**；埋点改动在本地待合入，审核过后提 0.2.2
- 真机 HTTP/2 keepalive 缺陷已修（服务端 --timeout-keep-alive 75 + nginx proxy_next_upstream），前端无需再动

**建议待办（按优先级）**：
1. **0.2.2 合入埋点提审**：page_views 埋点代码已写好，等 0.2.1 审核结果后合入
2. **错题本深化**：weaknesses 表数据已有，做错题重练流（quiz 从 weaknesses 拉题）
3. **学习进度可视化**：moyan:last 已存进度；补章节进度条/连续学习天数
4. **个人页**：绑定学习档案展示（turns 统计、审错率趋势），后端已有 /api/admin/stats 数据口径可仿
5. **小程序内改名入口**：rename 端点已含 AI 审核（422 时向用户展示拒绝理由）
6. **跨章检索前端开关**：VEC-04 教学注入默认关，可在设置页加开关（管理员可见）

**小程序端铁律**：
- 新增 wx.request 域名必须 https 且在小程序后台配置过
- 代码同源双端：平台差异全部走条件编译，禁止 fork 出第二份逻辑
- 个人主体无支付、教育类目受限：不要做收费/课程表等敏感类目功能

## 5B. 网页端（独立开发线二）

**代码**：`frontend-web/`（独立 Vue3+Vite 工程；dev `npm run dev`，build 产物部署 /var/www/moyan-web）
**生产**：https://moyan.baimo7715.top （nginx 静态 + /api 反代 5001）

**现状**：
- 免登录（首次访问自动获得 web_anon/web_<did> 身份）、书架、上传（429 限流 5/h）、搜索、教学聊天页（fetch+ReadableStream SSE）、管理台 /admin
- 最近完成：删除收敛管理台、重命名 AI 审核、用量台账页

**建议待办（按优先级）**：
1. **教学体验打磨**：聊天气泡渲染（当前 text-delta 纯文本拼接，可做 markdown/公式/代码块）；判定徽章/题卡交互升级
2. **错题本页面**：weaknesses 数据已有，做错题列表+重练
3. **学习报告页**：report 事件已有数据；做周报/知识图谱
4. **注册/登录增强**：现在纯免登录，跨设备进度丢失；可做可选的邮箱绑定（backend 有邮箱密码鉴权旧实现可参考 commit 44659cb）
5. **上传体验**：docling 解析 5-6 分钟，前端只有 pollTask 轮询文字；做进度流/排队提示
6. **管理台增强**：ai_usage 台账已有，加图表（按天/按用户聚合）
7. **向量检索**：VEC-04 默认关，可做管理员开关 UI + 检索命中展示

**网页端铁律**：
- 所有请求带 X-Device-Id（localStorage 持久化），匿名身份是共享书库的 key
- 写操作（改名/上传）有 AI 审核或限流，前端必须处理 422（展示 reason）和 429（Retry-After）
- SSE 用 fetch+ReadableStream，不要换 EventSource（POST 不支持）

---

## 6. 后端 API 契约速查（双端通用）

```
POST /api/auth/wx-login {code} → JWT            # 小程序
POST /api/auth/dev-login                        # 仅 AUTH_DISABLED=1
GET  /api/auth/me                               # 当前身份
GET  /api/documents?q=                          # 书架(支持分词 AND 搜索)
POST /api/documents/upload (multipart)          # 限流 5/h；content_hash 去重 reused:true 秒回
POST /api/documents/{doc_id}/rename {title}     # 非 admin 过 AI 审核；同名短路 unchanged:true；限流 10/min
DELETE /api/documents/{doc_id}                  # 仅 admin
POST /api/tutor/start {doc_id} → {session_id, greeting,...}
POST /api/tutor/turn {session_id, user_text}    # SSE 流（见第 2 节协议）
GET  /api/admin/usage /api/admin/stats          # 台账/统计（admin）
GET  /api/admin/smoke                           # 冒烟探针记录（admin）
POST /api/admin/documents/{id}/share {shared}   # 一键下架/恢复（admin）
GET  /api/privacy                               # 隐私与保留策略（免鉴权）
POST /api/admin/login {password}                # 管理台 30 天 JWT，限流 10/min
```

- 鉴权：`Authorization: Bearer <jwt>`；网页端可不带，由 get_requester 落 X-Device-Id/web_anon
- 写端点限流：L_TUTOR 30/min、L_UPLOAD 5/h、rename 10/min；429 带 Retry-After
- **新增任何会花钱的 AI 端点必须挂限流 + ai_usage 记账**（改端点漏限流曾被刷账单，边界测试 R11）

---

## 7. 关键坑（血泪，必读）

1. **Edit 大文件后必须验证类归属**：曾把 chat_stream 挤出 Provider 类成模块级函数（py_compile 不报错），生产讲解流坏一天。**改引擎核心文件后跑一条真链 SSE 才算闭环**；回归锁在 test_engine_softening.py::test_provider_chat_stream_is_method
2. **app_settings 单例共享**：backend.tests.* 跨文件共享，测试里改配置一律 monkeypatch.setattr，绝不依赖模块级 setdefault
3. **slowapi 限流装饰器**：要求函数第一参 `request: Request`（+Response）；压测必须带合法 JWT（401 在依赖层抛出，endpoint 不计数）
4. **SQLite→PG 方言差异**：JSON 列 NULL 过滤陷阱；create_all 不给已有表加列（生产加列要显式迁移）
5. **生产 .env 是 CRLF**：bash source 会炸；提取单变量用 `grep+cut+tr -d '\r'`
6. **backend/db.py 是死代码勿用**（config.DATABASE_URL 不存在）；生产查库 `sudo -u postgres psql moyan`
7. **服务器同域**：主域 baimo7715.top =「隙」社交平台(gunicorn:5000)；redis 被隙依赖**勿停**；同服务器部署前先探测端口+server_name 归属
8. **部署前必查 requirements.txt**（曾漏 slowapi/PyJWT 致服务器 ModuleNotFoundError）；补传文件后用 md5 对比回归（out/_hash_compare.py）
9. **docling 必须离线**：沙箱/服务器代理环境会给 docling 子进程注入 http_proxy 致 ProxyError；docling_adapter._run_worker 已剥代理，别动
10. **反代 keepalive ≥ proxy_read_timeout**：否则 wx/H2 端随机 TypeError:network error（已修：uvicorn --timeout-keep-alive 75）。反代实为 **nginx**（/etc/nginx/sites-available/moyan）；服务器虽装 Caddy 但服务 failed 已弃用，deploy/Caddyfile 是死文件勿参照
11. **测试锁不可破**：D11 安全网锁（test_d11_fixes 6 例：两题叠一/打码/skip 桥句）、边界锁（test_boundaries）、P0 回归锁。改引擎先跑全量 pytest
12. **git 状态**：远端=本地=生产（M4 交付后 ≥60019f2），push 已恢复；接手后 `git pull` 即可拿基线
13. **SEC-01 会话归属**：turn/resume/review-* 校验 owner，非 owner 一律 404（不暴露存在性）；同会话并发第二 turn 返 409「上一轮仍在进行中」——前端须把 409 当提示而非错误弹窗
14. **限流与熔断**：限流 key 匿名按 IP（同出口 IP 共享额度是有意行为）；日预算熔断 MOYAN_DAILY_TOKEN_BUDGET/​HARD 默认 0 关闭；SEC-02 所有 AI 调用带 max_tokens=4000（cheap 1500）
15. **书库 fail-closed + shared**：上传审核异常现在 503 拒收（回退开关 MOYAN_MODERATION_FAIL_OPEN=1）；documents.shared 列控制公共书架可见性，管理台可一键下架（POST /api/admin/documents/{id}/share）
16. **探针与迁移**：moyan-smoke.timer 每 5 分钟 health + 每 60 分钟 AI 全链探针（结果 /api/admin/smoke）；DB 变更一律走 alembic（生产 stamp/upgrade 已就绪），勿手写 ALTER

---

## 8. 环境/凭据指引

- 本地启动：`MOYAN_AUTH_DISABLED=0 MOYAN_JWT_SECRET=<32位> C:/Python314/python.exe run.py`（5001；vite dev 5173 代理 /api）
- **生产 venv 为 Python 3.12**（本文技术栈行写的 3.13 有误，以服务器为准）
- 生产凭据不在仓库（.env 不入库）；本地 out/_env_prod.txt、_pg_pass.txt 有副本（**待用户处置，用后删**）
- 部署脚本模板：out/_p45_upload.py（SFTP+md5）、out/_ssh_run2.py（SSH 执行器，get_pty=False 必须）
- AI 引擎配置：backend/settings.py（MOYAN_ 前缀环境变量）；AI 可 mock（MOYAN_AI_MOCK=1）供无密钥开发

## 9. 给接手团队的一天冲刺建议

两条线并行、互不依赖（只通过 API 契约耦合），推荐节奏：
1. **前 2h**：各自环境跑通（网页端 dev 起 5173 联本地 5001；小程序端 dev:mp-weixin 导入开发者工具），跑通一轮教学 SSE
2. **中间 6h**：按 5A/5B 待办第 1-3 项做功能；每完成一项跑 `pytest`（后端改动时）+ 真链验证
3. **收尾**：提交说明写清"改了什么/怎么验证的"，方便人工合并回本仓库
4. **红线**：不动 backend/engine 核心五层与 D11 安全网（除非先读第 7 节第 1 条）；不动生产部署；AI 调用类新功能挂限流+记账
