---
phase: 11
plan: 1
must_haves:
  - 教学气泡 markdown 渲染（markdown-it html:false 防 XSS），代码块/列表/加粗可见
  - 错题本页 + 重练流页（MistakesView/PracticeView，接 Phase 10 口径）
  - 学习报告页（ReportView，轻量 SVG 趋势）
  - 管理台用量柱状图 + 向量管理 UI（VEC-04 运行时开关 + 检索命中展示）
  - npm build 通过 + 部署 + frontend/ 零改动
---

# Plan 11-01 — 网页端功能升级

对应需求：MD-01/PRAC-02/RPT-02/CHART-01/VECUI-01。

## Tasks

### T1 MD-01 markdown 渲染
- 依赖：markdown-it（`html:false` 默认转义原始 HTML，防 XSS）
- TutorView：assistant 文本经 `md.render()` 渲染（v-html），流式期间增量重渲
- 用户消息保持纯文本

### T2 PRAC-02 错题本 + 重练
- `MistakesView.vue`：跨文档错题列表（GET /api/me/weaknesses，后端小改：owner 维度全量列表）
- `PracticeView.vue`：review-session 重练流（start→queue→answer→summary），按 doc 发起
- router + HomeView 入口

### T3 RPT-02 学习报告页
- `ReportView.vue`：接 /api/study/report，SVG 柱状趋势 + 薄弱分布徽章

### T4 CHART-01 管理台图表
- AdminView 用量节追加 SVG 柱状图（/api/admin/usage daily 数据，无新依赖）

### T5 VECUI-01 向量管理 UI
- 后端微调：`POST /api/admin/vec/config {vec_inject:bool}`（运行时开关，非持久化）+ GET status 已有
- AdminView 向量节：开关 + 按 doc 建索引按钮 + 检索命中列表

### T6 构建/部署
- npm run build；dist 全量上传；后端 vec/config 端点部署；人工冒烟清单
