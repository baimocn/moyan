---
phase: 12
plan: 1
must_haves:
  - 隐私告知：独立页 + 首次启动弹窗（moyan:privacy_ack 持久化）
  - 错题重练页 + 个人页（接 Phase 10 API）
  - 上传进度展示（pollTask progress 条）
  - npm run build:mp-weixin 编译通过；条件编译不破坏 H5；提审包语义（0.2.3）
---

# Plan 12-01 — 小程序升级

对应需求：PP-01/PRAC-03/ME-02/UP-02。**门禁：用户 2026-09-05 宣布执行即解冻**
（改动进下一提审版本，不影响在审 0.2.x）。

## Tasks

### T1 PP-01 隐私告知
- 新页 `pages/privacy/privacy.vue`（静态策略文本 + 同步 GET /api/privacy 可选）
- 首次启动 `pages/index/index.vue` onShow 检查 `moyan:privacy_ack`，无则弹窗引导；同意后落存储

### T2 PRAC-03 错题重练
- 新页 `pages/mistakes/mistakes.vue`：weaknesses 列表（owner 维度）+ 发起重练
  （review-session/start + answer，Bearer 鉴权走 utils/api.js）

### T3 ME-02 个人页
- 新页 `pages/profile/profile.vue`：GET /api/me/stats（turns/正确率趋势/薄弱分布/连续天数）

### T4 UP-02 上传进度
- index 上传流程：上传后轮询 /api/tasks/{id}，展示 progress 进度条

### T5 构建验证
- `npm run build:mp-weixin` 编译通过；pages.json 注册新页；提审说明（0.2.3 = 埋点 + 本阶段）
