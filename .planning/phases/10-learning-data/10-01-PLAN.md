---
phase: 10
plan: 1
must_haves:
  - doc 级 study 读端点（7 个）全部加身份+可见性，非 owner 且非 shared 一律 404
  - 错题本列表按 owner 维度 + due/mastery/times_low 排序（PRAC-01）
  - GET /api/study/report 与 GET /api/me/stats 聚合 API（RPT-01/ME-01，含正确率趋势/薄弱分布/连续天数）
  - 上传进度契约写入 API 契约第七节（UP-01）
  - pytest 全绿；frontend 零改动
---

# Plan 10-01 — 学习数据底座与遗留收口

对应需求：AUTH-04/PRAC-01/RPT-01/ME-01/UP-01。

## Tasks

### T1 AUTH-04 doc 级可见性收口
- `backend/models/repo.py` 加 `doc_visible(shared, owner, openid, role) -> bool`（纯函数）；
  documents.py `_doc_visible` 改为委托
- `backend/routers/study.py` 七个 doc 级 GET 全加 `user` 依赖：查 Document → 不可见 404

### T2 PRAC-01 错题本口径
- `repo.list_weaknesses(doc_id, user_id=None)` 加 owner 过滤（None=全部，兼容 admin/共享场景）
- `/api/study/{doc_id}/weaknesses` 传 user.openid（admin 不过滤）；排序 due_at NULLS LAST → mastery → times_low

### T3 RPT-01/ME-01 报告聚合
- `repo.user_report(openid, days=30) -> dict`：turns/sessions 计数、正确率趋势（近 N 日逐日
  avg(score)+count，Python 侧聚合避方言）、薄弱点分布（mastery 计数）、连续天数
  （复用 streak_from_dates，按 user turns 日期）
- `GET /api/study/report`（study.py）+ `GET /api/me/stats`（auth/router.py）双入口共用；
  web_anon 请求者返回 `scope_note` 显式标注共享匿名口径

### T4 UP-01 契约
- API 契约第七节追加 8.x：进度查询 GET /api/tasks/{id}（progress/done_pages）+ 轮询间隔建议（3-5s）

### T5 测试与回归
- `test_phase10.py`：doc 级 7 端点可见性（B 404/owner 200/匿名 shared 200）、错题 owner 过滤、
  report 结构与趋势字段、me/stats 身份口径
- 全量 pytest；部署 backend；契约同步
