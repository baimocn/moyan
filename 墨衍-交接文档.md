# 墨衍 · 模型迁移交接文档（v3，已同步 2026-08-29 全天进展）

> **开新对话时把本文件路径发给 AI**：`D:\Desktop\墨衍-项目\墨衍-交接文档.md`
> 新 AI 读取本文件 + `墨衍-项目规划.md` + `docs\墨衍-教学引擎设计文档.md` 即可无缝继续。
>
> **v3 = 模型迁移版（2026-08-28 深夜初版）**：把"当前系统形态 / 未完成工作 /
> 坑速查 / 环境索引"重组为自包含交接，**先读第四节（未完成工作）再读第二节（系统形态）**。
>
> **2026-08-29 深夜已同步全天进展**（详见会话导出 `墨衍-交接文档-v4-20260829.md`）：
> 6 份真题 PP-Structure 精修验收留用、教学引擎质量整改 5 真因、教学法深化四件套、
> 性能整改（裁判 158s→5.6s）、人物卡定案「同桌」、双前端地基（CORS+API契约）、
> 前端 v0 教学控制台、uni-app 双端骨架 + 小程序导入运行。测试 46/46。

---

## 一、项目一句话

「墨衍」AI 导师：学生上传课程资料（PDF/Word/PPT…）→ 自动转 MD 分章节 → AI 像老师一样
"讲解 → 苏格拉底式提问 → 判定 → 分支调整 → 章末考 → 记录薄弱点 → 复习自动带薄弱章节"。
**差异化：系统主导学习路径，不是问答框。**

## 二、当前系统形态（五引擎，全部实装且真机验证）

```
① 解析引擎   Docling(py3.13 子进程)＝主；文本层 PyMuPDF/RapidOCR＝legacy 兜底；
             PP-Structure/MinerU＝扫描/表格/公式按页精修（质量上限）
② AI 引擎    main=deepseek-v4-flash（讲解流式）；cheap=同网关（判定/出题/校对/裁判）；
             Router 主备 failover+熔断；MOYAN_AI_MOCK=1 且无 key 才全 mock（防假老师）
③ 教学引擎   FSM: explain→question→evaluate→分支→章末考；8硬/8软规则提示词；
             护栏：反馈防泄漏过滤(_sanitize_feedback) + 选项洗牌(shuffle_options)
             + 输出后裁判(reviewer, 5 判据, sample/on/off)
④ 记忆引擎   官方 py-fsrs 复习调度（卡片四态/稳定性/难度）；due 队列按"挽回记忆/分钟"代理排序；
             复习会话 engram 失败回收；概念→章节聚合
⑤ 优化引擎   Groove: strategy_logs 聚合"哪个讲法有效"；Tracer: /traces 学习轨迹；
             explain 用 best_strategy 数据驱动换讲法
```

**一轮教学数据流**：上传 → Docling→MD→切章 → start（章计划缓存，0s）→ turn：
讲解(流,main+出题 cheap,usage 并入 turns)→ 题目(单选,洗牌)→ 学生答 → judge(cheap+usage)
→ 反馈(防泄漏)→ 裁判采样 → 分支 → 薄弱点入账(FSRS+章节) → 策略反馈(Groove) → SSE 推送。

**关键决策存档**（勿推翻，除非架构师改口）：
- 主引擎维持 **deepseek-v4-flash**（横向评测已做，备选 glm-5.3/pro 数据存档
  `docs/主引擎横向评测-2026-08-28.md`；`tools/model_bench.py` 可复测）
- 解析主引擎 **Docling**；**不换**（PaddleOCR 全系评估见 `docs/PaddleOCR评估-2026-08-28.md`）
- 结构化输出用自研 `engine/structured.py`（替代 instructor，D1 修订，usage 可审计）

## 三、怎么跑

```bash
cd D:\Desktop\墨衍-项目
# 依赖：requirements.txt（系统 py3.14）；两个独立 venv（py3.13）：
#   .docling-venv：docling 2.123 = uv venv --python 3.13 .docling-venv
#                  && uv pip install --python .docling-venv\Scripts\python.exe docling
#   .ocr-venv：paddleocr 3.7（PP-Structure 精修用）= uv venv --python 3.13 .ocr-venv
#                  && uv pip install --python .ocr-venv\Scripts\python.exe paddlepaddle==3.3.1 paddleocr "paddlex[ocr]" pymupdf
python run.py                 # http://127.0.0.1:5001（/docs、/api/health）
python -m pytest backend/tests --basetemp=backend\tests\.pytest_tmp -p no:cacheprovider
```
- `.env` 含真实 key（见末尾）；**文档含明文 key，勿提交/勿外传**
- 服务无 reload，改代码重启；`MOYAN_AI_MOCK=1` 本地无 key 演示（需同时清空 MOYAN_AI_MAIN/CHEAP 相关，否则走真引擎）
- 常用开关：`MOYAN_PARSER_ENGINE=docling|legacy`；`MOYAN_TEACHING_REVIEWER=off|sample|on`

## 四、未完成工作（★迁移后第一优先级，按此接续）

### 4.1 【已完成·已验收留用（2026-08-29）】6 份自考真题的 PP-Structure 精修

- **两个根因已定位并修复**（E1/E2 判别实验 + `faulthandler` 抓现场，实验脚本在 `data/work/exam_pp_diag/`）：
  1. **300dpi 页图触发 PP-OCRv5_server_det 原生段崩**（access violation @ text_detection predictor → paddle_static runner，无 Python 异常可接，退出码 139）。150/200dpi 同页正常 → **v2 默认 200dpi**。此前「日志只有模型加载」系 stderr 尾部截断误判，崩溃点其实在首次 predict。
  2. **旧脚本字段名假设错误**：`parsing_res_list` 块字段实测是 `label/content/bbox/index`，并非 `block_label/block_content/block_order`（旧写法全取空）→ worker 已按实测修正。
- **工具 v2**：`tools/exam_ppstructure_md.py`（主控）+ `tools/exam_pp_worker.py`（worker）。特性：逐 PDF subprocess 隔离（段崩只死 worker）→ 缺页自动降 150dpi 逐页重试 → 仍失败写占位并报告；页脚「试题第N页(共M页)」正则兜底剔除；工作目录纯 ASCII（`data/work/exam_pp_v2/`）；逐页 JSON 片段支持断点续跑（重跑只补缺页）。
  运行：`.ocr-venv\Scripts\python.exe tools\exam_ppstructure_md.py "真题文件夹"`（`--max-pages` 调试）。
- **A/B 结论（《数据库》4 页，vs Docling 版）**：结构大改善——Q22 题干从乱码「2←←=费 码是」恢复为真实内容、Q8/Q10 选项错乱编号消除、公式 LaTeX 化、标题/部分顺序正确、「本题10分」修正；小瑕疵仍在——「插人」仅修 1/3（识别模型层，留 D8 AI 校对）、选项行合并（A.xB.y 挤一行，Docling 反而分行）、密封区碎片残留、个别新错字（候i 码/诊包括）、填空空位成「0」。
- **Docling 版 6 份 MD 已备份** `D:\Desktop\26年4月计算机科学与技术真题_docling备份\`，全量可回滚；**2026-08-29 架构师验收：PP-Structure 版留用**。
  当日 v2.1 重拼（`tools/exam_pp_rehead.py`）：标题块→`#`/`##`、密封区碎片剔除——正文未动，供教学引擎切章/规划用。
- **全量 6 份（28 页）2026-08-29 完成：28/28 成功、0 失败、纯推理约 48 分钟**（《数据结构》13.6min 最慢——公式块多）。两点运行观察：
  - 《高级语言》第 7 页曾遇一次**偶发**原生退出（0x40000004，非 300dpi 那种确定性崩），
    重跑同页 200dpi 直接成功 → 崩溃有「确定性（300dpi）/偶发」两类，v2 重试机制都能兜住；
  - 中途会话中断把整棵后台进程树带走（0 字节日志、无常驻 python），恢复运行凭逐页片段
    **1.8 分钟补完**——断点续跑设计已实战验证。
- 六份成品在真题文件夹（同名 .md 已替换为 PP-Structure 版），逐页片段在 `data/work/exam_pp_v2/`。

### 4.2 次优先级待办（按顺序）
1. 【P1·已修复 2026-08-29】**教学引擎质量整改**（前后对照实录：`docs/教学实录-数据库真题-2026-08-29.md` 修复前 / `…-修复后.md`）。
   真机坐实的真因与修复（中途两个错误假设已纠正：洗牌答案键有重映射是好的；"归一化丢字段"
   不成立，SSE 事件字段名 `correctness` 是设计如此）：
   - 判定误判主因：`JUDGE_INSTRUCTION` 只传正确答案字母、**不传选项文本** → 模型幻觉
     "选项和解释不一致"，把答对判 partial（输出后裁判其实抓到了并写了 note，但 passed=true
     只落库不处置）。已修：提示词注入【选项清单】+ 评分对齐口径 + skip 反馈不悬空提问。
   - 记忆链饿死主因：判定提示词从未要求 weak_points（模型恒回空）且代码无兜底 →
     薄弱点/FSRS/章节聚合全 0。已修：提示词强制 + `judge.py::_harmonize` 兜底。
   - score 恒 0.5（模型留默认）：`_harmonize` 按分档对齐。
   - 知识点计划「全文」退化：plan LLM 失败**静默落 mock**（真因=`KnowledgePlan.chapter_id/
     chapter_title` 必填而模型常漏 → 校验重试耗尽）叠加无标题 MD。已修：schema 改默认空、
     落 mock 打 warning、MD 重拼出标题（`tools/exam_pp_rehead.py`，doc_title→#、
     paragraph_title→##、aside_text 密封区剔除；6 份已重写回真题文件夹）。
   - `repo.add_judgement` 读 `correctness_level` 而事件 payload 键是 `correctness` → 审计列恒空。已修。
   - 修复后验证：单测 38/38；真机 `s_0b5b073e32` 字母答对 correct/1.0/skip、答错
     incorrect/0.1 + `db/architecture/physical-independence:low` 落档，review_due 0→1。
2. 【P2·已修复 2026-08-29 下午】**讲解文本可泄漏下一题选项字母**：教师提示词新增硬规则 9
   （讲解不得指认选项字母，出题会重洗牌，指认必然误导）。
3. 【已实装 2026-08-29 下午·架构师拍板"按你的结果优化"】**教学法深化四件套**（调研见
   `docs/调研-GitHub脚手架与苏格拉底项目-2026-08-29.md`，真机验证会话 `s_0b5b073e32`）：
   - **程序级脚手架阶梯**：TutorSession.hint_level(0-3) 进 FSM+DB（teaching_sessions 加列）；
     答错/部分对升级、答对降级（fading）、skip 归零、施压≥3 强制 3 级（D3 归并）；
     judge 按级生成 feedback.hint，不越级泄答案。实测：连错两次反馈从"指向性"升到"直接对比定义"。
   - **裁判四维质量评分**（AITutor-EvalKit 分类学）：reviewer 输出 scores
     （mistake_identification/mistake_location/providing_guidance/actionability），
     任一维 <0.5 显式发 meta guard（此前 note 只落库不处置）。实测 0.7/0.6/0.9/0.8。
   - **socrates-skill 并入**：教师提示词五类提问手法（澄清/追问/联结/反诘/假设）+ 收尾
     请学生总结；出题首题诊断式。实测出现假设式反例题（"小明认为…最有力的反例是？"）。
   - **施压检测扩五类**（2607.19371）：直接要答案/逐步套取/角色重构/情绪施压模式。
   - **章末考空上下文修复**：_start_exam 按 skill_id→知识点名→fetch_context 传考题上下文。
     实测考题靶向薄弱点（迁移场景考物理独立性）。
   - **连带修复两个连带 bug**：①出题键-文错位（模型偶发 correct_answer 键与选项文字不符，
     洗牌忠实保留错误 → 判定把答对判不一致；已加 correct_answer_text 校对+自动改键+warning）
     ②correct_answer 字符串容错（"D"→["D"]，before validator）③sanitize 豁免 skip 分支
     （答对推进后关键词不再被抹成 □）④**章末考中间态续学**（exam_questions/exam_idx/
     exam_scores 落库恢复——重启不再出"掌握度 0%"空报告）。
   - 已知遗留：重讲解时可能复述正确选项的概念原文（真题卷特有，概念即选项文字；教材类无此问题）；
     sanitize 的 \b[A-F]\b 会误伤教材里的关系名 S（P3，仅影响美观）。
4. 【性能·已实装 2026-08-29 晚】**串行链并行化 + 裁判提速**（实测会话 `s_49b933d66c` 等）：
   - 讲解轮：出题与讲解流并行（asyncio.create_task），串行空等消除——38.3s → 24.7s
     （quiz_ms=23.2s 与流重叠，总时长=两者取 max）；
   - 裁判三连修：①切 cheap 直连（原走主引擎）②note 限 40 字且仅异常时输出
     ③采样改"首判不审，第 6/11/… 次审 + 高风险必审"——**review_ms 158s → 5.6s（-96%）**；
   - 巩固题与裁判并行；SQLite 开 WAL+busy_timeout+synchronous=NORMAL（多用户并发写地基）；
   - **常驻性能观测**：每轮判定 meta 带 judge_ms/review_ms/quiz_ms（本轮实测 judge 7s/26s/30s/
     32s 波动，网关是最大变量）——后续优化先看这份数据；
   - 连带修两个自查 bug：QuestionSpec 挂 latency_ms 触发 pydantic setattr 老坑（重犯，已按
     "先定义字段"修复）；judge.py 结构被改坏 _mock_judge 嵌进模块函数（真机撞到，已 AST 校验修复）。
5b. 【试聊 4 缺陷·已修复 2026-08-29 深夜】①两题叠一：教师提示词改"讲解正文不以提问结尾，
    检查理解由系统出题负责"（真机验证：讲解不再追加追问）②净化器只遮正确项文字（干扰项保留，
    指向性提示可读）③skip 桥句（"这题过了。下一站：「X」"）+ 章末考过渡 ④讲解态插话收束
    （"你插的那句我记下了"——真机验证生效）。单测 46/46。
5. 【人物设计·定案「同桌」+ 试聊实测 2026-08-29 晚】**导师人物卡（D11，留存抓手）**：经四轮否案
   （老师→三形态→尖锐感→分化三形态均被否）定案为**同桌**——坐你旁边备考同门的人，
   不是服务者：有自己的错题本/模考分/错题要问（关系双向）。角色卡 V2 五件套 +
   世界书（CHARACTER_BOOK 按章节关键词滴灌同桌旧事进上下文）；开场白三态 + 连续天数
   TA 的口吻。唯一事实源 `backend/engine/persona.py`（改人设只改这里）；
   单测 46/46；真机语气已验。迭代史见 `docs/人物设计-D11-墨衍人物卡-2026-08-29.md`。**试聊 7 轮（架构师亲测）抓出 4 缺陷待修**：①两题叠一（讲解追问 vs 正式单选，2 次）
②净化器全遮选项词致提示不可读 ③skip 无过渡报站 ④讲解态输入无回执——修法见
D11 文档试聊档案节；演示端错题/伪造选项各 1 次的教训：**前端必须原样渲染题目事件选项**。
6. 复习"复习即考"（用 judge/quiz cheap 打分替代自评，D4 缺口）；
7. 判定/出题 usage 偶发缺失（网关行为，字段缺省 0）；切章器把 `<!-- 页注释 -->` 当标题（ch0 碎章）；
8. 【双前端地基·已实装 2026-08-29 晚】CORS 白名单（MOYAN_CORS_ORIGINS，默认空=仅同源，
   安全默认不变）+ 小程序接入契约（wx.request enableChunked 读同一 SSE 流，零后端改动）+
   真机联调绑定说明（MOYAN_HOST=0.0.0.0）。**接入手册：`docs/API契约-双前端-2026-08-29.md`
   （端点清单/事件协议表/小程序样例代码/上线约束）**；单测 46/46（含 2 个 CORS 用例）。
8c. 【uni-app 骨架·已实装 2026-08-29 深夜】`frontend/`（Vue3+Vite，@dcloudio 3.0.0-alpha-5020520260824002 全家桶，
    手工脚手架免 degit）：选课页 + 同桌聊天页（复刻控制台交互），传输层条件编译
    （H5=fetch 流式 / MP-WEIXIN=wx.request enableChunked，同一事件协议）；H5 与
    mp-weixin 双目标**构建全过**（mp 包在 frontend/dist/build/mp-weixin，直接导入微信开发者工具；
    H5 dev 5173 + /api 代理已验证连通后端）。启动：`cd frontend && npm run dev:h5`（或 dev:mp-weixin）。
    验收进展（2026-08-29 深夜）：**mp-weixin 包已导入开发者工具并运行**——项目「墨衍」已打开，
    模拟器渲染选课页成功（教材/章节/开始学习 + 同桌提示，urlCheck=false 已随构建包生效）。
    交互层自动化受阻：CUA 坐标派发对该 Electron 窗口报 identity mismatch（元素通道正常但
    模拟器区域无 a11y 元素），且人机共用鼠标互相干扰。**剩余一步（人工 2 分钟）**：
    开发者工具里点选教材「26.4 数据库原理与技术(1).md」→ 章节「数据库原理与技术」→ 开始学习
    → 在聊天页发一句话，验证同桌开场白与教学流。package-lock 已生成。
8b. 【前端 v0·已实装 2026-08-29 晚】**快速教学控制台** `backend/static/tutor.html`
   （浏览器开 http://127.0.0.1:5001/tutor.html ，零依赖单文件，静态挂载免重启）：
   选教材/章节 → start（渲染 greeting+知识点路线图）→ SSE 流式聊天（打字机、判定徽章、
   裁判四维、**题目选项原样渲染可点选**——落实"前端铁律"、章末考批量、掌握度报告）+
   学习档案侧栏（薄弱点/待复习/tokens）+ 续学按钮。已知未修：试聊 4 缺陷仍会显现
   （两题叠一/□打码/无报站）——前端照实渲染引擎事件，修复待架构师拍板。
   下一步：uni-app H5（同一套 API，SSE 双适配）。
9. gpt-5.6-luna 网关稳定后复测（主引擎备选补全）。

### 4.3 进程/服务状态（迁移时）
- 5001 服务在本会话是后台 job 跑着最新代码；**迁移重启进程会杀掉它**，接手后按需 `python run.py` 重起。
  （2026-08-29 深夜补记：接手方确认 **5001 正在运行**（PID 34800，最新代码，真引擎），
  仅会话中断/重启后会掉，`python run.py` 随时可起。前端 v0 控制台 `http://127.0.0.1:5001/tutor.html` 可直接访问。）
- 微信开发者工具：2026-08-29 深夜验收时项目「墨衍」已导入并运行（模拟器渲染选课页成功）；
  **当前进程未开**，交互验收（四-1，人工 2 分钟）需重开工具。
- 5001 上方会有历史数据（《普通生物学》两版已导入 DB；操作系统 docing 版可开课）。

### 4.4 外部等待事项（非工程，但用户会问）
- **GitHub 旧账号 `baimocn` 2FA 解封**：2026-08-29（今夜）通过官方流程
  `github.com/login → 登录(密码过) → sessions/two-factor/recovery → "Begin account or email recovery"`
  重新提交：①邮箱 OTP 验证 ✅ ②当前设备验证 ✅ ③申请已提交（"Device verified and request
  submitted!"）；**等待 GitHub Support 审核 1-3 个工作日**，跟进邮件发往账号关联邮箱
  （2630647415@qq.com 等）。⚠️ **审核期间勿再点"取消/重复提交"**——页面明示额外请求
  不会被审核（上次就是误点取消作废重来的）。恢复后建议：改密码（密码曾在聊天共享过）+ 重设
  规范 2FA（认证器/恢复码文件 `github-recovery-codes.txt`）。新账号 `baimo7715-creator` 正常。

## 五、成果速览（全部真机验证；细节见对应 docs/）

| 模块 | 状态 | 关键文件/证据 |
|---|---|---|
| 解析：Docling 主引擎（12 格式） | ✅ | `backend/services/docling_adapter.py`、`routers/upload.py`、`tools/docling_worker.py`（pypdfium2 后端）；`docs/全类型转换调研与实测-2026-08-28.md` |
| 扫描 OCR 三角 | ✅ | RapidOCR(2.7-4s) → Docling(14-38s) → PP-Structure(90-183s)；`docs/PaddleOCR评估-2026-08-28.md` |
| 教学引擎 FSM + 护栏 | ✅ | `engine/tutor/*`；防泄漏滤+洗牌+裁判；真实引擎验收 `docs/真实引擎验收-2026-08-28.md` |
| 结构化输出（D1 修订） | ✅ | `engine/structured.py`（json_object+围栏剥离+别名归一化+usage）；判定/出题/校对全真 |
| 记忆：FSRS + 复习会话 | ✅ | `models/repo.py`（官方 py-fsrs）、`engine/review/service.py` |
| 优化：Groove/Tracer | ✅ | `strategy_logs` 表 + `/strategy-stats` + `/traces` + 数据驱动换讲法 |
| 用量审计 | ✅ | turns.usage + 判定 usage；stats.tokens |
| 质量回归 | ✅ | 单测 46/46；`tools/guardrail_run.py`（真实引擎 5 用例，按需跑，耗 token） |
| 主引擎评测 | ✅ 已拍板 | `docs/主引擎横向评测-2026-08-28.md`（维持 flash；备选存档） |
| 六份真题 Docling 转 MD | ✅ | `D:\Desktop\26年4月计算机科学与技术真题\*.md`（Docling 300dpi 版；备份在 `…_docling备份\`） |
| 六份真题 PP-Structure 精修 | ✅ v2 跑通 | `tools/exam_ppstructure_md.py`+`exam_pp_worker.py`（200dpi+段崩隔离）；A/B 结论见 4.1 |
| 导师人物卡「同桌」（D11） | ✅ 定案实装 | `backend/engine/persona.py`（V2 五件套+世界书）；`docs/人物设计-D11-墨衍人物卡-2026-08-29.md` |
| 双前端地基（CORS+契约） | ✅ | `backend/main.py` CORS 白名单 + `docs/API契约-双前端-2026-08-29.md`（小程序 enableChunked 样例） |
| 前端 v0 教学控制台 | ✅ | `backend/static/tutor.html`（SSE 流式/判定徽章/选项点选/章节路线图/档案/续学）；46/46 |
| uni-app 双端骨架 | ✅ 构建全过 | `frontend/`（Vue3+Vite @dcloudio alpha）；H5 5173 + mp-weixin 包 `frontend/dist/build/mp-weixin` |
| 小程序导入验收 | ⏳ 剩 2 分钟人工 | 项目「墨衍」已导入运行；交互验收步骤见 4.2-8c / 4.1-四 |

## 六、坑速查（新模型必读，别重踩）

**本会话新增（2026-08-29 深夜，前端/工具链）**：
- pydantic 禁 setattr 未定义字段（重犯第二次）→ 挂运行时值必须先在 schema 定义（exclude=True）；
- 写 PowerShell `.ps1` 含中文必须带 BOM（或全 ASCII），否则 PS5 按 ANSI 读→语法炸；
- bash heredoc 内嵌 Python 转义：`\\n` 到目标文件变真换行——生成代码用脚本文件，勿内嵌大段；
- curl 发中文 JSON：`-d` 会被本地码页坑，用 `--data-binary @file`（文件以 UTF-8 写）；
- Git Bash 的 /tmp 与 Windows Python 路径不通 → 临时文件放项目内；
- 乱码显示别当真：Git Bash ls/管道乱码≠数据坏，用 Python `os.listdir`/ascii() 权威确认；
- **CUA 对 Electron 窗口（微信开发者工具）**：坐标派发报 identity mismatch；元素通道 AXPress 正常但
  模拟器区域无 a11y 元素；人机共用鼠标互扰 → 动态窗口矩形相对定位可用，需窗口静置；
- **WeChat DevTools CLI**：服务端口关闭时 CLI 不可用；`echo y |` 喂不进确认（非 TTY）→
  GUI 设置→安全设置 开启（localstorage `security.enableServicePort` 可直改，改后重 launch）；
- tasklist 过滤中文进程名用 `//FI "IMAGENAME eq 微信开发者工具.exe"`；杀进程前先 `netstat -ano`
  确认端口归属，勿盲杀。

**本会话新增（2026-08-29，精修诊断）**：
- PPStructureV3 对 **300dpi 页图**文本检测推理原生段崩（无 Python 异常）→ 用 200dpi；
  原生崩查现场必带 `faulthandler.enable()`；「无产出退出」先查退出码 139/0xC0000005；
- `tail -c` 只看日志尾部会误判崩溃点（当时误判为 FormulaNet 加载崩），要读全量日志；
- paddlex `parsing_res_list` 块字段是 `label/content/bbox/index`（按 `block_*` 取全空）；
- 杀后台 job 主进程不保证杀掉 subprocess 子进程，清理产物前先 `tasklist` 核对孤儿。

**本会话新增（2026-08-28）**：
- pydantic 模型不许 `setattr` 未定义字段 → 要挂运行时值（如 usage）必须先在 schema 定义字段（`exclude=True`）；
- 小模型会自拟 JSON 字段名（`stem`→`question`、`question_type`→`type`、选项 `label`/字符串）→
  `structured._apply_aliases(aliases, list_renames)` 归一化，**校验失败会静默落 mock**（最危险），真机必带 usage 探真；
- `SplitResult` 无 `headings`（upload/tasks 各踩一次）；
- paddle 3.3.1 Win CPU 必须 `PADDLE_PDX_ENABLE_MKLDNN_BYDEFAULT=False`；
- docling-parse 对中文路径崩 → 强制 `PyPdfiumDocumentBackend`；
- 测试环境：`%TEMP%\pytest-of-*` 或项目内 basetemp 被残留进程锁 → 全量测试会"无输出挂起"；
  解法：杀残留 python 进程 + 删 `.pytest_tmp`，用 `--basetemp` 后**后台跑**（等 job 完成）更稳；
  **别乱杀 python 进程**（上次误杀 5001 服务进程）；
- 起 5011/5001 测试服时，若只想 mock，必须清空 `MOYAN_AI_MAIN_*/CHEAP_*/FALLBACK_*` 环境变量
  （.env 里有真实 key，会真烧钱）。pwsh 下空串 env 会被丢弃、要写脚本里 os.environ 清空。

**老坑（保留）**：SQLite 时间 naive→`_utc()`；instrucor 已整体移除（勿再引）；`config.py` 只有路径+复读；
reteach_count 清零只能在答对/skip（防连错保护失效）；question_id 必须由调用方指定。

## 七、环境与资产索引

- 三个 python：系统 3.14（后端）；`.docling-venv`（py3.13, docling）；`.ocr-venv`（py3.13, paddle/paddlex/pymupdf）
- 依赖：`requirements.txt`（后端）；两个 venv 安装命令见第三节
- 工具：`tools/` -- docling_worker / ocr_convert / import_legacy_docs / guardrail_run /
  model_bench / exam_folder_md（Docling 批转）/ exam_ppstructure_md（PP-Structure 精修 v2 主控）
  + exam_pp_worker（逐页 worker，200dpi、段崩隔离、断点续跑）
- 数据：`data/`（上传/切章/工作区）；DB `data/moyan_dev.db`（SQLite）7 张表
- 文档：`docs/`（教学引擎设计 D1-D10、真实引擎验收、PaddleOCR评估、全类型转换调研、主引擎横向评测）
- 参考项目：`参考项目/`（cangjie-skill蒸馏=教学法技能包方法论备选；synapse-skills=优化引擎灵感已吸收；
  PaddleOCR-PDF=源码归档）

## 八、硬约束（务必读）

- 用户是架构师，只决策不执行；**问题一次只问一个**；要指正不要假夸；成本意识强（省 token）
- 教学铁律：不罗列、不直接给答案（宽松=先思路后结论）、只依据教材、苏格拉底、程序级防"支架崩塌"
- 本机：Windows + Python 3.14；沙箱禁 multiprocessing 命名管道（用 subprocess+文件通信）；
  网关 opencode.ai 需浏览器 UA（openai SDK 实测可用）
- 文档与 `.env` 含真实密钥：**勿提交、勿外传、勿打印到公开渠道**

## 九、下一步候选（等你拍板；接盘方优先从 4.2-8c / v4 四-1 接手）

1. **微信开发者工具交互验收**（剩人工 2 分钟，见 4.2-8c）：重开工具 → 选教材「26.4 数据库原理与技术(1).md」
   → 章节「数据库原理与技术」→ 开始学习 → 聊天页发一句话，验证同桌开场白与教学流
2. **同桌试聊第二轮回归**（4 缺陷修复后，重点：讲解不尾随追问/提示不□/换站有报站/插话有回执）
3. 部署件三步：鉴权（wx.login code2session→users）+ 每用户限流（免费限量）+ HTTPS/备案域名
   + CORS 收紧正式域名（当前 API 无鉴权，仅本机/可信环境）
4. 教学引擎优化收尾：复习即考（judge/quiz cheap 打分替代自评）、usage 偶发缺失补齐、
   gpt-5.6-luna 网关稳定后复测主引擎备选
5. 部署 2G 服务器

## 十、OpenGo 密钥（.env，已生效；字段名注意）

```
MOYAN_AI_MAIN_BASE_URL=https://opencode.ai/zen/go/v1
MOYAN_AI_MAIN_KEY=sk-4XzDZgbkQJmowpwYTOX1c6TRYV2qHH9cguWq7qRr32Z54i22371iXkFOEejUByse
MOYAN_AI_MAIN_MODEL=deepseek-v4-flash
MOYAN_AI_CHEAP_* 同网关同模型
```
（`MAIN_KEY` 不是 `MAIN_API_KEY`——pydantic-settings 字段映射坑；`.env` 绝对路径读，见 settings.py）